import setuptools

setuptools.setup(
    name="clip_search",
    version="0.1",
    author="Your Name",
    author_email="your_email@example.com",
    description="A simple project for searching through a database of image embeddings using OpenAI's CLIP model",
    packages=setuptools.find_packages(),
    python_requires=">=3.6",
)
